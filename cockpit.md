# /cockpit — Show Cockpit tasks

Display the current Cockpit task list.

## Instructions

Run `python3 /home/eyifrah/cockpit` and display the output as-is.

Do not summarize, reformat, or filter. Show the raw output from the script.

## Rules

- Never ask the user to run the command themselves — always run it directly
